- clean: ['~']

- link: 
    ~/.zshrc: zsh/zshrc
    ~/.ssh/config:
      path: ssh/config
      create: true
    ~/Library/Fonts/:  
      glob: true
      create: true
      path: fonts/distr/ttf/*

- shell:
  - [git submodule update --init --recursive, Installing submodules]